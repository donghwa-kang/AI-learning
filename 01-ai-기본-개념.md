# AI 공부 노트 01 — AI의 기본 개념

## 학습 방향

AI를 공부한다는 것은 단순히 ChatGPT API를 사용하는 법을 배우는 것이 아닙니다.

다음 세 가지를 단계적으로 이해하는 과정입니다.

- AI가 무엇인지 이해하기
- AI 모델이 데이터를 통해 학습하는 원리 이해하기
- AI를 실제 서비스로 만드는 방법 배우기

전체 흐름:

```text
AI 기본 개념 → 데이터와 머신러닝 → 딥러닝 → 생성형 AI → LLM → RAG와 Tool Use → AI 백엔드 → 실제 프로젝트
```

Python 문법은 별도로 공부하므로, 이 학습에서는 Python 문법 자체를 길게 다루지 않습니다. AI 개념을 설명하기 위해 필요한 코드는 간단히 사용할 수 있습니다.

---

# 1장. AI란 무엇인가

## 1.1 AI의 의미

AI는 Artificial Intelligence의 약자로, 한국어로는 인공지능입니다.

인공지능이란 일반적으로 다음과 같은 능력을 컴퓨터가 수행하도록 만드는 기술입니다.

- 정보를 이해하기
- 패턴 찾기
- 분류하기
- 예측하기
- 의사결정하기
- 언어 생성하기
- 이미지나 음성 이해하기
- 새로운 결과 만들기

예시:

- 사진 속에 고양이가 있는지 판단하기
- 이메일이 스팸인지 아닌지 분류하기
- 내일의 판매량 예측하기
- 음성을 글자로 변환하기
- 질문에 자연어로 답변하기
- 이미지를 설명하는 문장 만들기
- 사용자의 취향에 맞는 상품 추천하기

다만 AI가 사람처럼 모든 것을 이해한다는 의미는 아닙니다. 대부분의 AI는 특정한 작업을 잘하도록 만들어진 시스템입니다.

## 1.2 일반 프로그램과 AI의 차이

### 일반적인 프로그램

일반적인 프로그램은 사람이 직접 규칙을 작성합니다.

예를 들어 비밀번호를 검사하는 프로그램은 다음과 같은 규칙을 가질 수 있습니다.

- 비밀번호 길이가 8자 이상인가?
- 숫자가 포함되어 있는가?
- 대문자가 포함되어 있는가?

사람이 규칙을 명확하게 알고 코드로 작성해야 합니다.

### AI 프로그램

AI는 사람이 모든 규칙을 직접 작성하지 않습니다. 대신 많은 데이터를 제공하고, 데이터 속에 있는 패턴을 모델이 찾도록 합니다.

```text
많은 이메일 데이터 → 각 이메일이 스팸인지 아닌지 학습 → 새로운 이메일의 스팸 여부 예측
```

핵심 차이:

- 일반 프로그램: 사람이 규칙을 작성하고 컴퓨터가 실행합니다.
- AI 모델: 데이터에서 패턴을 학습하고 새로운 입력에 대해 예측합니다.

## 1.3 AI는 마법이 아니다

AI는 마법처럼 보일 수 있지만 내부적으로는 대략 다음 과정을 수행합니다.

```text
입력 데이터 → 숫자 형태로 변환 → 모델 내부 계산 → 가능성이 높은 결과 선택 → 결과 반환
```

AI의 결과에 영향을 주는 요소:

- 학습에 사용된 데이터
- 모델의 구조
- 모델의 크기
- 입력된 질문
- 제공된 추가 문서
- 모델의 설정
- 외부 도구 사용 여부

따라서 AI도 틀릴 수 있습니다.

---

# 2장. AI, 머신러닝, 딥러닝의 관계

## 2.1 인공지능 AI

AI는 가장 넓은 개념입니다. 컴퓨터가 지능적인 작업을 수행하도록 만드는 모든 방법을 포함합니다.

AI에는 반드시 머신러닝이 필요한 것은 아닙니다. 예를 들어 ‘온도가 30도 이상이면 에어컨을 켠다’와 같은 규칙 기반 시스템도 넓은 의미의 AI 시스템으로 볼 수 있습니다.

## 2.2 머신러닝 Machine Learning

머신러닝은 AI를 구현하는 방법 중 하나입니다.

머신러닝에서는 사람이 모든 규칙을 직접 작성하지 않고, 데이터에서 규칙이나 패턴을 학습합니다.

예시:

```text
이메일 내용 + 스팸 여부 → 머신러닝 모델 학습 → 새로운 이메일의 스팸 여부 예측
```

머신러닝의 핵심 질문:

> 데이터를 보고 새로운 데이터에 대한 결과를 예측할 수 있는가?

## 2.3 딥러닝 Deep Learning

딥러닝은 머신러닝의 한 종류입니다. 인공신경망을 여러 층으로 쌓아 복잡한 패턴을 학습합니다.

관계:

```text
AI ⊃ 머신러닝 ⊃ 딥러닝
```

딥러닝은 다음 작업에 특히 강합니다.

- 이미지 인식
- 음성 인식
- 자연어 처리
- 번역
- 자율주행
- 생성형 AI

전통적인 머신러닝에서는 사람이 중요한 특성을 직접 골라야 하는 경우가 많습니다. 반면 딥러닝은 원시 데이터에서 중요한 특성을 자동으로 찾아내는 능력이 뛰어납니다.

---

# 3장. 생성형 AI란 무엇인가

## 3.1 생성형 AI의 의미

생성형 AI는 기존 데이터를 단순히 분류하거나 예측하는 것을 넘어 새로운 결과를 만들어내는 AI입니다.

생성 가능한 결과:

- 텍스트
- 이미지
- 음성
- 음악
- 동영상
- 코드

예시:

```text
입력: 고양이에 대한 동화를 써줘
출력: 새로운 동화
```

생성형 AI는 학습한 데이터의 패턴을 바탕으로 새로운 결과를 조합합니다.

## 3.2 생성형 AI와 예측형 AI

예측형 AI의 예시:

- 이 리뷰는 긍정적인가?
- 이 이메일은 스팸인가?
- 다음 달 매출은 얼마인가?

생성형 AI의 예시:

- 이 제품 리뷰를 3문장으로 요약해줘.
- 고객에게 보낼 답변을 작성해줘.
- 이 내용을 표로 정리해줘.

핵심 차이:

- 예측형 AI: 기존 입력을 바탕으로 분류하거나 값을 예측합니다.
- 생성형 AI: 입력을 바탕으로 새로운 텍스트, 이미지, 코드 등을 만듭니다.

---

# 4장. LLM이란 무엇인가

## 4.1 LLM의 의미

LLM은 Large Language Model의 약자로, 대규모 언어 모델을 의미합니다.

LLM은 매우 많은 텍스트 데이터를 학습하여 다음과 같은 작업을 수행할 수 있습니다.

- 질문에 답하기
- 글 요약하기
- 번역하기
- 문장 수정하기
- 코드 작성하기
- 대화하기
- 문서에서 정보 추출하기
- 특정 형식의 결과 만들기

## 4.2 LLM의 기본 작동 방식

LLM의 작동 과정은 대략 다음과 같습니다.

```text
문장 입력 → 토큰으로 분리 → 토큰을 숫자 벡터로 변환 → 문맥 관계 계산 → 다음 토큰의 확률 계산 → 토큰을 반복적으로 생성 → 최종 문장 반환
```

LLM은 문장을 인간처럼 완벽히 이해한다고 단정하기보다, 문맥상 이어질 가능성이 높은 토큰을 계산하여 답변을 생성한다고 이해하는 것이 좋습니다.

## 4.3 LLM은 검색 엔진과 다르다

검색 엔진은 기존 웹페이지나 데이터베이스에서 정보를 찾아 보여주는 시스템입니다. LLM은 학습한 패턴을 이용해 답변을 생성하는 모델입니다.

차이:

- 검색 엔진: 문서 검색이 중심입니다.
- LLM: 생성된 답변이 중심입니다.
- 검색 엔진: 검색 시 최신 정보가 반영될 수 있습니다.
- LLM: 기본적으로 학습 시점과 제공된 문맥의 영향을 받습니다.

따라서 최신 정보나 정확한 사내 문서에 답변해야 할 때는 LLM만 사용하는 것이 아니라 검색 시스템이나 데이터베이스와 함께 사용해야 합니다. 이것이 나중에 배울 RAG와 연결됩니다.

---

# 5장. 머신러닝 모델이 학습하는 과정

머신러닝을 이해하려면 다음 다섯 가지 개념을 알아야 합니다.

```text
데이터 → 모델 → 예측 → 오차 계산 → 모델 수정
```

## 5.1 데이터

데이터는 모델이 학습할 자료입니다.

예를 들어 집값을 예측한다면 다음 정보가 필요합니다.

- 면적
- 방 개수
- 위치 점수
- 실제 가격

여기서 면적, 방 개수, 위치 점수는 입력 특성이고, 실제 가격은 정답 또는 레이블입니다.

## 5.2 모델

모델은 입력을 결과로 바꾸는 계산 구조입니다.

```text
면적 + 방 개수 + 위치 → 모델 → 예상 가격
```

모델은 처음에는 정확하지 않습니다.

## 5.3 예측

모델이 입력을 보고 결과를 계산합니다.

예시:

- 입력: 70㎡, 방 2개, 위치 점수 85
- 예측: 4억 8천만 원
- 실제 가격: 5억 원

예측과 실제 결과 사이에는 오차가 있습니다.

## 5.4 손실 Loss

손실은 모델의 예측이 얼마나 틀렸는지를 나타내는 값입니다.

모델은 학습 과정에서 손실을 줄이는 방향으로 수정됩니다.

기본 원칙:

```text
손실이 작을수록 좋은 예측입니다.
```

## 5.5 학습

모델은 다음 과정을 반복합니다.

1. 데이터를 입력합니다.
2. 예측합니다.
3. 실제 정답과 비교합니다.
4. 오차를 계산합니다.
5. 모델 내부 값을 조금 수정합니다.
6. 다시 예측합니다.

이 과정을 매우 많이 반복하면 모델이 데이터 속 패턴을 점점 잘 학습하게 됩니다.

---

# 6장. 학습과 추론

## 6.1 학습 Training

학습은 모델이 데이터를 보고 내부 값을 조정하는 과정입니다.

```text
데이터 + 정답 → 모델 수정
```

학습에는 많은 데이터와 계산 자원이 필요할 수 있습니다.

## 6.2 추론 Inference

추론은 이미 학습된 모델에 새로운 입력을 주고 결과를 얻는 과정입니다.

```text
새로운 입력 → 학습된 모델 → 예측 결과
```

ChatGPT에 질문을 입력하고 답변을 받는 것은 일반적으로 추론 과정입니다.

차이:

- 학습: 모델을 개선하는 과정입니다.
- 추론: 학습된 모델을 사용해 결과를 얻는 과정입니다.

AI 서비스를 개발할 때는 대부분 이미 학습된 모델을 API로 호출하여 추론을 수행합니다.

---

# 7장. AI가 틀리는 이유

AI는 항상 정답을 보장하지 않습니다.

주요 원인:

1. 학습 데이터의 한계

   모델이 학습하지 못한 정보는 정확히 알기 어렵습니다.

2. 최신 정보 부족

   모델의 학습 시점 이후에 발생한 사건은 모를 수 있습니다.

3. 애매한 질문

   질문이 모호하면 모델은 여러 해석 중 하나를 선택합니다.

4. 확률 기반 생성

   LLM은 항상 논리적으로 검증된 답을 생성하는 것이 아니라, 문맥상 가능성이 높은 토큰을 생성합니다.

5. 환각 Hallucination

   LLM이 사실이 아닌 내용을 그럴듯하게 만들어내는 현상입니다.

예시:

- 존재하지 않는 논문을 실제 논문처럼 설명합니다.
- 없는 법률 조항을 있다고 말합니다.
- 존재하지 않는 함수를 사용한 코드를 작성합니다.

실제 서비스에서 필요한 장치:

- 출처 제공
- 검색 시스템 연결
- 답변 가능 여부 판단
- 사용자 확인
- 결과 검증
- 로그와 모니터링

---

# 8장. AI 모델 개발과 AI 서비스 개발

## AI 모델을 만드는 공부

- 수학
- 머신러닝 알고리즘
- 딥러닝
- PyTorch
- 모델 학습
- 데이터셋
- GPU
- 논문

## AI 서비스를 만드는 공부

- Python
- API
- FastAPI
- LLM API 호출
- 프롬프트 설계
- 데이터베이스
- RAG
- 인증
- 배포
- 비용 관리

첨부된 phase1-2.md는 주로 두 번째 분야인 AI 서비스 개발과 백엔드에 해당합니다.

---

# 9장. 전체 학습 로드맵

## 1단계: AI 기본 개념

- AI, 머신러닝, 딥러닝의 차이
- 생성형 AI와 LLM
- 학습과 추론
- 데이터, 모델, 예측, 손실

## 2단계: 데이터와 머신러닝

- 특성과 레이블
- 훈련 데이터와 테스트 데이터
- 회귀와 분류
- 과적합과 과소적합
- 모델 평가

## 3단계: 딥러닝

- 인공신경망
- 가중치와 편향
- 손실 함수
- 경사하강법
- 역전파
- Transformer

## 4단계: 생성형 AI와 LLM

- 토큰
- 임베딩
- Attention
- 프롬프트
- 컨텍스트
- API 호출
- 스트리밍

## 5단계: AI 서비스 개발

- FastAPI
- 대화 기록
- 세션 관리
- RAG
- 벡터 검색
- Tool Use
- 에러 처리
- 비용과 보안

---

# 오늘의 핵심 정리

- AI는 컴퓨터가 지능적인 작업을 수행하도록 하는 넓은 개념입니다.
- 머신러닝은 데이터에서 패턴을 학습하는 AI 방법입니다.
- 딥러닝은 인공신경망을 사용하는 머신러닝 방법입니다.
- 생성형 AI는 새로운 텍스트, 이미지, 코드 등을 생성합니다.
- LLM은 대량의 텍스트를 학습한 언어 모델입니다.
- 학습은 모델이 데이터를 보고 내부 값을 조정하는 과정입니다.
- 추론은 학습된 모델에 새로운 입력을 주고 결과를 얻는 과정입니다.
- 손실은 모델의 예측이 얼마나 틀렸는지 나타내는 값입니다.
- LLM은 확률적으로 답변을 생성하므로 틀릴 수 있습니다.

---

# 확인 문제

## 문제 1

머신러닝에 가장 가까운 것은 무엇일까요?

1. 사람이 모든 조건문을 직접 작성합니다.
2. 많은 이메일 데이터를 보고 스팸 여부를 학습합니다.
3. 정해진 계산식을 그대로 실행합니다.

정답: 2번

## 문제 2

ChatGPT에 질문하고 답변을 받는 과정은 학습일까요, 추론일까요?

정답: 추론입니다.

## 문제 3

LLM이 틀릴 수 있는 이유를 두 가지 적어 보세요.

예시 답변:

- 학습 데이터에 정보가 없을 수 있습니다.
- 최신 정보를 모를 수 있습니다.
- 확률적으로 문장을 생성하기 때문에 환각이 발생할 수 있습니다.

## 문제 4

다음 관계를 완성해 보세요.

```text
AI ⊃ 머신러닝 ⊃ 딥러닝
```

정답: 딥러닝은 머신러닝의 한 종류이고, 머신러닝은 AI의 한 종류입니다.

---

# 다음 학습 내용

다음에는 데이터, 특성, 레이블, 모델, 예측을 자세히 배웁니다.

핵심 질문:

> AI 모델은 데이터를 어떻게 보고, 무엇을 기준으로 잘 학습했다고 판단하는가?
